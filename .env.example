DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jr_brasil_integrado?schema=public"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Credenciais OAuth da ACBr — são da APLICAÇÃO (plataforma/SaaS), não por empresa.
# Configure uma vez; todas as empresas emitem com elas. Cada empresa é registrada na ACBr (POST /empresas) + certificado.
ACBR_CLIENT_ID=""
ACBR_CLIENT_SECRET=""
